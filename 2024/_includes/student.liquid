---
layout: base
---

<div class="full-width">
	<img class="main-image" src="/2024/static/media/{{ main_image }}" cf-src="{{ main_image }}/xl" alt="{{ project_title }}" />
</div>

<div class="meta">
	<div>
		<h2>{{ project_title }}</h2>
        <h3>{{ student_name }}</h3>
	</div>
	<div>
		<p class="year">{{ context }}&nbsp;&nbsp;{{ year }}</p>
	</div>
</div>

<article>
	{{ content }}
</article>

<div class="social-links__wrapper">
	<div class="social-links">
		{% for link in social_links %}
		<a class="social-link" target="_blank" href="{{ link }}">{{ link | replace: "https://", "" }}</a>
		{% endfor %}
	</div>
</div>

{%- for image in images -%}
<img class="image" src="/2024/static/media/{{ image }}" cf-src="{{ image }}/xl" alt="" />
{%- endfor -%}

